import 'package:flutter/material.dart';

class CustomerAppointmentList extends StatelessWidget {
  const CustomerAppointmentList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox();
  }
}
